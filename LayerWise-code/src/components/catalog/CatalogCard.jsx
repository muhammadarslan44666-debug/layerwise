import { Link } from 'react-router-dom';
import { Printer, CircleDot, ArrowUpRight } from 'lucide-react';
export default function CatalogCard({record,kind}){const printer=kind==='printers';return <Link to={printer?`/printers/${record.manufacturer_slug}/${record.slug}/`:`/filaments/${record.slug}/`} className="panel group flex flex-col p-6 transition-colors hover:border-orange-300"><div className="mb-6 flex items-center justify-between"><span className="flex h-14 w-14 items-center justify-center rounded-xl bg-gray-50 text-gray-600">{printer?<Printer strokeWidth={1.3} size={30}/>:<CircleDot strokeWidth={1.3} size={32}/>}</span><ArrowUpRight size={16} className="text-gray-400 group-hover:text-orange-600"/></div><p className="text-[10px] font-semibold uppercase tracking-widest text-gray-400">{printer?record.manufacturer:'Material family'}</p><h2 className="my-2 font-heading text-xl font-bold">{printer?record.model:record.material}</h2><p className="mb-5 text-sm leading-6 text-gray-500">{printer?record.build_volume||'Documentation and calibration references. Unchecked specifications are not published.':record.description}</p><span className="mt-auto text-[10px] text-gray-500">{record.verification_status?.replaceAll('_',' ')} · {record.last_verified||'No check date'}</span></Link>;}
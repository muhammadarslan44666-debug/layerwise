import { Link } from 'react-router-dom';
import { Printer, ArrowUpRight, CircleDot } from 'lucide-react';
import useCatalog from '@/components/data/useCatalog';
import SectionHeading from '@/components/shared/SectionHeading';
export default function CatalogPreview(){const {printers,loading,error}=useCatalog();return <section className="container-page section-space"><SectionHeading eyebrow="BUILT AROUND YOUR SETUP" title="Your printer. Your starting point." link="/printers/" label="Browse printers" description="Manufacturer references, relevant tests, and honest limitations."/>{loading?<p className="text-sm text-gray-500">Loading printer references…</p>:error?<p role="alert">Printer references could not load. Please refresh to try again.</p>:<div className="grid gap-5 md:grid-cols-3">{printers.map((p,i)=><Link to={`/printers/${p.manufacturer_slug}/${p.slug}/`} key={p.id} className="group panel flex items-center gap-5 p-6 hover:border-orange-300"><div className={`flex h-20 w-20 shrink-0 items-center justify-center rounded-lg ${i%2?'bg-[#f1f2f3]':'bg-[#f2f3ef]'}`}><Printer size={43} strokeWidth={1.1} className="text-gray-500"/></div><div><p className="text-[9px] font-bold uppercase tracking-widest text-gray-400">{p.manufacturer}</p><h3 className="mt-2 font-heading text-lg font-bold">{p.model.replace('Original Prusa ','')}</h3><span className="mt-2 inline-flex items-center gap-1 text-[10px] text-gray-500">Explore reference<ArrowUpRight size={11}/></span></div></Link>)}</div>}<div className="mt-12 flex flex-wrap items-center justify-between gap-5"><div><h3 className="font-heading text-lg font-bold">What’s on your spool?</h3><p className="mt-1 text-xs text-gray-500">Start with the material. Calibrate for the exact product.</p></div><div className="flex flex-wrap gap-2">{['PLA','PETG','ABS','ASA','TPU','Nylon','PC'].map((m,i)=><Link to={`/filaments/${m.toLowerCase()}/`} className="flex items-center gap-2 rounded-lg border border-gray-200 px-3 py-2.5 text-[11px] font-semibold hover:border-orange-300" key={m}><CircleDot size={15} className={['text-emerald-600','text-blue-500','text-gray-600','text-amber-600','text-violet-500','text-teal-600','text-slate-500'][i]}/>{m}</Link>)}</div></div></section>;}
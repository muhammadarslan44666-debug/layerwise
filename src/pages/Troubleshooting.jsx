import { useState } from 'react';
import { Search } from 'lucide-react';
import PageIntro from '@/components/shared/PageIntro';
import ProblemCard from '@/components/cards/ProblemCard';
import { problems } from '@/components/data/problems';
export default function Troubleshooting(){const [q,setQ]=useState('');const filtered=problems.filter(p=>`${p.name} ${p.short} ${p.description}`.toLowerCase().includes(q.toLowerCase()));return <div className="container-page py-10 pb-20"><PageIntro eyebrow="PROBLEM → TEST → PROGRESS" title="What’s going wrong with your print?" description="Start with what you see. Each diagnosis connects the basic checks to a specific test, explains how to read it, and helps you decide what to change next."/><div className="relative mb-8 max-w-lg"><Search size={18} className="absolute left-4 top-3.5 text-gray-400"/><input aria-label="Filter problems" className="field !pl-11" placeholder="Describe a symptom…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-5">{filtered.map(p=><ProblemCard key={p.slug} problem={p}/>)}</div>{!filtered.length&&<p className="py-10 text-sm text-gray-500">No matching symptom. Try “gaps”, “corners”, “strings” or clear the search.</p>}<p className="mt-8 text-xs leading-6 text-gray-500">These workflows concern filament-based printing. Mechanical checks use your model’s documentation; no universal firmware or safety settings are supplied.</p></div>;}